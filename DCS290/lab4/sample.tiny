/* this is a comment line in the sample program */
INT f2(INT x, INT y ) 
BEGIN 
    INT z;
    z := x*x - y*y + 0x123;
    RETURN z; 
END
INT MAIN f1(REF INT a0, REAL a2) 
BEGIN
    INT x;
    READ(x, "A41.input");
    INT y;
    READ(y, "A42.input");
    INT z;
    z := f2(x,y) + f2(y,x);
    WRITE (z, "A4.output"); 
END
